# media

Domain package for phase 2: `MediaItem`, `MediaList`, status enums and their services and tables.
Intentionally empty in the initial scaffold. Routes will live in `../api`, DTO mirrors in
`frontend/src/types/api.ts`.
