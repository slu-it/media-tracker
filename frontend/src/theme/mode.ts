/** localStorage key MUI's ThemeProvider persists the chosen mode under ("light" | "dark" | "system").
 *  backend/src/main/resources/login/login.html hard-codes the same key so the login page follows the choice. */
export const MODE_STORAGE_KEY = "mt.mode";
