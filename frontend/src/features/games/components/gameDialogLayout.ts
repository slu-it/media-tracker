/** Shared height for the add/view/edit game dialogs, so switching between modes never resizes the dialog. */
export const GAME_DIALOG_HEIGHT = 640;

/** Shared height for the add/view/edit expansion dialog, so switching between modes never resizes the dialog. */
export const EXPANSION_DIALOG_HEIGHT = 460;
